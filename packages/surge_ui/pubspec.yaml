name: surge_ui
description: Surge Studios shared UI toolbox — token contract, theme, and the presentational component library every Surge app composes from.
version: 0.4.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
