name: surge_import_queue
description: Share-sheet intake queue (Tier 3 System) — durable inbox, allowance-aware drain planning, and drain coalescing for content shared into the app, extracted from Ladle's share extension pipeline.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
