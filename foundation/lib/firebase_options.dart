import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

/// PLACEHOLDER. Running `flutterfire configure` overwrites this file with the
/// real per-platform options for your Firebase project. Until then it throws if
/// used, so a Firebase build fails loudly rather than silently misconfiguring.
/// The default (mock) build never touches this file — see bootstrap.dart.
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform => throw UnimplementedError(
    'Run `flutterfire configure` to generate firebase_options.dart, then set '
    'useFirebase = true in bootstrap.dart.',
  );
}
