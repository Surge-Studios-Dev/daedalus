/// Surge core — shared pure-Dart utilities.
///
/// Everything here is plain Dart (no Flutter imports) so it can be used by
/// any tier: core app logic, tests, tools. Current contents: the DST-safe
/// ISO-date helpers every planner/streak/meter feature ends up needing.
library;

export 'src/dates.dart';
