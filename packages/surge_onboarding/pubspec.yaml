name: surge_onboarding
description: Drop-in onboarding flow (Tier 3 System) — a data-driven, themed intro carousel built on surge_ui. The app owns persistence and routing; this owns the flow.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  surge_ui:
    path: ../surge_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
