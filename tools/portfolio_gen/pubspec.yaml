name: portfolio_gen
description: Generates a Surge marketing-site portfolio entry (a PortfolioProject) seeded from an app's surge.manifest.yaml. Narrative fields come out as TODO placeholders to fill.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  yaml: ^3.1.2

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
