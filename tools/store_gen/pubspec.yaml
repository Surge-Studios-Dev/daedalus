name: store_gen
description: Generates Fastlane deliver (iOS) and supply (Android) store-metadata trees from a surge.manifest.yaml store block, validating store character limits.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  yaml: ^3.1.2

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
