# surge_ui gallery

The human index of the component library (FRAMEWORK.md): every surge_ui
component rendered live, with a light/dark toggle and a theme-pack
switcher (palette icon) so each pack can be checked against each
component before it ships in an app.

```
flutter run -d macos          # fastest look
flutter run -d <ios sim id>   # what users actually see
```
