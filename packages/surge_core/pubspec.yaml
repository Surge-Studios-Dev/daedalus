name: surge_core
description: Shared pure-Dart core utilities — DST-safe ISO-date helpers extracted from Ladle. No Flutter imports; safe for any tier to depend on.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
